import React from "react";

function NavTabs () {
    return(
        <div className= "container">
            <jumbotron>
                <h1>Drag Clicky Game</h1>

                <p>Only click the faces once, and don't F*** it up.</p> 

            </jumbotron>
        </div>
    )
}

export default NavTabs;